<Page x:Class="Klimalauf.Import2"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Klimalauf"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Import2">
    <Grid  DataContext="{StaticResource imodel}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <local:TopBar x:Name="Leiste" Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Top" />

        <DataGrid ItemsSource="{Binding KlasseItems}"  Grid.Row="1" x:Name="Klassen_Grid"
          AutoGenerateColumns="False">
            <DataGrid.Resources>

                <Style TargetType="ComboBox" x:Key="RundenArtenStyle">
                    <Setter Property="ItemsSource"
                    Value="{Binding RelativeSource={RelativeSource
                                FindAncestor, 
                                AncestorType={x:Type Window}},
                                Path=DataContext.RundenArten}"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Bezeichnung" Binding="{Binding Bezeichnung}" Width="200"/>

                <DataGridComboBoxColumn Header="Rundenart" Width="75"
                                SelectedItemBinding="{Binding SelectedRundenArt}"
                                ElementStyle="{StaticResource RundenArtenStyle}"
                                EditingElementStyle="{StaticResource RundenArtenStyle}"/>
            </DataGrid.Columns>
        </DataGrid>


        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,5,10,10" HorizontalAlignment="Right">
            <Button Content="Importieren" x:Name="btnWeiter" Width="100" Height="25" Margin="5,5,10,5"/>
            <Button Content="Zurück" x:Name="btnBack" Width="100" Height="25" Margin="5"/>
        </StackPanel>
    </Grid>
</Page>
