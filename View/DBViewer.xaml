<Page x:Class="RunTrack.DBViewer"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RunTrack"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DBViewer">
	<Page.Resources>
		<local:DBViewerModel x:Key="viewModel"/>
	</Page.Resources>
	<Grid DataContext="{StaticResource viewModel}">
		<TabControl ItemsSource="{Binding TableNames}" SelectedItem="{Binding SelectedTable}">
			<TabControl.ItemTemplate>
				<DataTemplate>
					<TextBlock Text="{Binding}" />
				</DataTemplate>
			</TabControl.ItemTemplate>
			<TabControl.ContentTemplate>
				<DataTemplate>
					<DataGrid ItemsSource="{Binding DataContext.CurrentTableData.DefaultView, RelativeSource={RelativeSource AncestorType=TabControl}}" 
                              AutoGenerateColumns="True" />
				</DataTemplate>
			</TabControl.ContentTemplate>
		</TabControl>
	</Grid>
</Page>
